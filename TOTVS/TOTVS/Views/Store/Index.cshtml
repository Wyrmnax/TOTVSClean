@model IEnumerable<TOTVS.Models.Genre>
@{
    ViewBag.Title = "Store";
}
<h3>Vendo Generos</h3>
<p>
    Escolha entre os Generos:
</p>
<ul>
    @foreach (var genre in Model)
    {
        <li>
            @Html.ActionLink(genre.Name,
"Browse", new { genre = genre.Name })
        </li>
    }
</ul>